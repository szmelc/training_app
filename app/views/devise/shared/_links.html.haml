- if devise_mapping.omniauthable?
  - resource_class.omniauth_providers.each do |provider|
    = link_to image_tag(('google_sign_in.png'), class: 'btn btn-google'), omniauth_authorize_path(resource_name, provider)
    %br/

- if controller_name != 'sessions'
  .btn.btn-primary.btn-shared
    = link_to "Log in", new_session_path(resource_name)

- if devise_mapping.registerable? && controller_name != 'registrations'
  .btn.btn-primary.btn-shared
    = link_to "Sign up", new_registration_path(resource_name)

- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations'
  .btn.btn-primary.btn-shared
    = link_to "Forgot password?", new_password_path(resource_name)
  %br/

- if devise_mapping.confirmable? && controller_name != 'confirmations'
  .btn.btn-primary.btn-shared
    = link_to "Confirmation instructions?", new_confirmation_path(resource_name)
  %br/

- if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks'
  = link_to "Didn't receive unlock instructions?", new_unlock_path(resource_name), class: 'btn btn-primary'
  %br/


